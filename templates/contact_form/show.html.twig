{% extends 'base.html.twig' %}

{% block title %}ContactFormEntity{% endblock %}

{% block body %}
    <h1>ContactFormEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_form_entity.id }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ contact_form_entity.Subject }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact_form_entity.Email }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ contact_form_entity.Body }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contact_form_index') }}">back to list</a>

    <a href="{{ path('contact_form_edit', {'id': contact_form_entity.id}) }}">edit</a>

    {{ include('contact_form/_delete_form.html.twig') }}
{% endblock %}
